@model BugBustersHR.BLL.ViewModels.AdminViewModel.GetManagerListVM

@{
    ViewData["Title"] = "ManagerEdit";
}
<style>

</style>

<div class="container-fluid mt-6 px-md-4 d-flex justify-content-end align-items-center">
    <div class="col-10">
        @*<input type="file" asp-for="BackgroundImageModel.File" class="form-control" id="backgroundImageInput" style="display:none;" />
        <label for="backgroundImageInput" class="btn btn-outline-dark btn-sm">Upload Background Image</label>*@
        <div class="page-header min-height-300 border-radius-xl" id="backgroundImage" style="background-image: url('https://images.unsplash.com/photo-1531512073830-ba890ca4eba2?ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&ixlib=rb-1.2.1&auto=format&fit=crop&w=1920&q=80');">
            <span class="mask bg-gradient-secondary opacity-6"></span>
        </div>
    </div>
</div>
<div class="toast-container position-fixed top-0 end-0 p-3 mt-5" style="z-index: 1051;">
    <div class="toast fade bg-light text-dark border" role="alert" aria-live="assertive" id="successToast" aria-atomic="true">
        <div class="toast-header">
            <i class="material-icons text-success me-2">check</i>
            <strong class="me-auto font-weight-bold">Bug Busters</strong>
            <small class="text-muted">Just Now</small>
            <button type="button" class="btn-close" data-bs-dismiss="toast" aria-label="Close"></button>
        </div>
        <div class="toast-body">
            The changes are saved!
        </div>
    </div>
</div>
<div class="container-fluid px-2 px-md-4 d-flex justify-content-end align-items-center mt-n11 bg-light" style="min-height: 100vh">
    <div class="col-10 mt-n12 bg-light">
        <div class="card card-body mx-3 mx-md-4 bg-light opacity-9">
            <div class="card-header pb-0 p-3 bg-light">
                <div class="row">
                    <div class="col-md-12 d-flex justify-content-center align-items-center">
                        <h2 class="mb-0">Manager Edit</h2>
                    </div>
                </div>
            </div>
            <form asp-area="Admin" asp-controller="Default" asp-action="ManagerEdit" method="post" enctype="multipart/form-data" id="editProfileForm">
                <input type="hidden" asp-for="Id" />
                <div class="row justify-content-center align-items-center mb-2">
                    <div class="avatar avatar-xxl position-relative">
                        <img id="profileImage" src="@Model.ImageUrl" alt="profile_image" class="w-100 border-radius-lg shadow-sm">
                        <input type="file" asp-for="ImageModel.File" accept="image/*" class="form-control" id="photoInput" style="display:none;" onchange="updateProfileImage()">
                    </div>
                </div>
                <div class="row">
                    <div class="col-md-3">
                        <!-- Column 1 -->
                        <div class="form-floating mb-3">
                            <input asp-for="Name" class="form-control shadow-sm" aria-required="true" placeholder="Name" />
                            <label asp-for="Name">Name</label>
                            <span asp-validation-for="Name" class="text-danger"></span>
                        </div>
                        <div class="form-floating mb-3">
                            <input asp-for="SecondName" class="form-control shadow-sm" placeholder="SecondName" />
                            <label asp-for="SecondName">SecondName</label>
                            <span asp-validation-for="SecondName" class="text-danger"></span>
                        </div>
                        <div class="form-floating mb-3">
                            <input asp-for="Surname" class="form-control shadow-sm" placeholder="Surname" />
                            <label asp-for="Surname">Surname</label>
                            <span asp-validation-for="Surname" class="text-danger"></span>
                        </div>
                        <div class="form-floating mb-3">
                            <input asp-for="SecondSurname" class="form-control shadow-sm" placeholder="SecondSurname" />
                            <label asp-for="SecondSurname">SecondSurname</label>
                            <span asp-validation-for="SecondSurname" class="text-danger"></span>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <!-- Column 2 -->
                        <div class="form-floating mb-3">
                            <input asp-for="TC" class="form-control shadow-sm" placeholder="TC" />
                            <label asp-for="TC">TC</label>
                            <span asp-validation-for="TC" class="text-danger"></span>
                        </div>
                        <div class="form-floating mb-3">
                            <input asp-for="BirthPlace" class="form-control shadow-sm" placeholder="BirthPlace" />
                            <label asp-for="BirthPlace">BirthPlace</label>
                            <span asp-validation-for="BirthPlace" class="text-danger"></span>
                        </div>
                        <div class="form-floating mb-3">
                            <input asp-for="BirthDate" class="form-control shadow-sm" placeholder="BirthDate" type="date" />
                            <label asp-for="BirthDate">BirthDate</label>
                            <span asp-validation-for="BirthDate" class="text-danger"></span>
                        </div>

                        <div class="form-floating mb-3">
                            <input asp-for="ResignationDate" class="form-control shadow-sm" placeholder="ResignationDate" type="date" />
                            <label asp-for="ResignationDate">Resignation Date</label>
                            <span asp-validation-for="ResignationDate" class="text-danger"></span>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <!-- Column 3 -->
                        <div class="form-floating mb-3">
                            <input asp-for="CompanyName" class="form-control shadow-sm" placeholder="CompanyName" />
                            <label asp-for="CompanyName">Company Name</label>
                            <span asp-validation-for="CompanyName" class="text-danger"></span>
                        </div>


                        <div class="form-floating mb-3">
                            <input asp-for="SalaryString" class="form-control shadow-sm" placeholder="Salary" />
                            <label asp-for="SalaryString">Salary</label>
                            <span asp-validation-for="SalaryString" class="text-danger"></span>
                        </div>
                        <div class="form-floating mb-3">
                            <input asp-for="Title" class="form-control shadow-sm" placeholder="Title" />
                            <label asp-for="Title">Title</label>
                            <span asp-validation-for="Title" class="text-danger"></span>
                        </div>

                        <div class="form-floating mb-3">
                            <input asp-for="Section" class="form-control shadow-sm" placeholder="Section" />
                            <label asp-for="Section">Section</label>
                            <span asp-validation-for="Section" class="text-danger"></span>
                        </div>

                    </div>
                    <div class="col-md-3">
                        <!-- Column 4 -->

                        <div class="form-floating mb-3">
                            <input asp-for="TelephoneNumber" class="form-control shadow-sm" placeholder="TelephoneNumber" />
                            <label asp-for="TelephoneNumber">TelephoneNumber</label>
                            <span asp-validation-for="TelephoneNumber" class="text-danger"></span>
                        </div>

                        <div class="form-floating mb-3">
                            <textarea asp-for="Address" class="form-control shadow-sm" rows="5" style="resize: none;" placeholder="Address"></textarea>
                            <label asp-for="Address">Address</label>
                            <span asp-validation-for="Address" class="text-danger"></span>
                        </div>
                       
                    </div>
                </div>
                <hr />
                <div class="row">
                    <div class="col-sm-3 text-center">
                        <label for="photoInput" class="btn btn-outline-dark btn-light">
                            <i class="fas fa-upload"></i> Upload Photo
                        </label>
                    </div>
                    <div class="col-sm-3 text-center">
                        <button id="updateButton" type="submit" class="btn btn-outline-dark btn-light">
                            <i class="fas fa-save"></i> Save
                        </button> &nbsp
                    </div>
                    <div class="col-sm-3 text-center">
                        <a asp-area="Admin" asp-controller="Default" asp-action="GetManagerDetail" class="btn btn-outline-dark btn-light">
                            <i class="fas fa-info-circle"></i> Detailed Info
                        </a>
                    </div>
                    <div class="col-sm-3 text-center">
                        <a asp-area="Admin" asp-controller="Default" asp-action="Index" class="btn btn-dark btn-outline-secondary text-lg-center">
                            <i class="fas fa-home"></i> Home
                        </a>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>

<script>
    function updateProfileImage() {
        var profileImage = document.getElementById("profileImage");
        var photoInput = document.getElementById("photoInput");

        if (photoInput.files && photoInput.files[0]) {
            var reader = new FileReader();

            reader.onload = function (e) {
                profileImage.src = e.target.result;
            };

            reader.readAsDataURL(photoInput.files[0]);
        }

        validateAndUpdateButton(); // Update the button status after image change
    }



    function showSuccessToast() {
        var validationSummary = document.getElementById('validationSummary');
        if (validationSummary != null && validationSummary.classList.contains('validation-summary-errors')) {
            // If there are validation errors, do not submit the form
            return;
        }

        // Show the toast notification
        var toast = new bootstrap.Toast(document.getElementById('successToast'));
        toast.show();

        // Wait for a few seconds (adjust the delay as needed)
        setTimeout(function () {
            // Submit the form
            document.getElementById('editProfileForm').submit();
        }, 4000); // 4 seconds delay, you can change this to your preferred value
    }

    // Find all buttons with the toast-btn class
    var toastButtons = document.getElementsByClassName('toast-btn');
    for (var i = 0; i < toastButtons.length; i++) {
        toastButtons[i].addEventListener('click', showSuccessToast);
    }

    var telephoneNumberInput = document.getElementById("telephoneNumber");
    telephoneNumberInput.addEventListener("input", validateAndUpdateButton);
    var addressInput = document.getElementById("Address");
    addressInput.addEventListener("input", validateAndUpdateButton);
</script>



<script>
    document.addEventListener("DOMContentLoaded", function () {

        var telephoneNumberInput = document.getElementById("TelephoneNumber");
        var addressInput = document.getElementById("Address");
        var photoInput = document.getElementById("photoInput");
        var updateButton = document.getElementById("updateButton");

       

        validateAndUpdateButton();

        telephoneNumberInput.addEventListener("input", validateAndUpdateButton);
        addressInput.addEventListener("input", validateAndUpdateButton);
        photoInput.addEventListener("change", validateAndUpdateButton);

        function validateAndUpdateButton() {
            var isValidTelephoneNumber = validatePhoneNumber(telephoneNumberInput.value);
            var isValidAddress = validateAddress(addressInput.value);
            var isImageChanged = isFileSelected(photoInput);

            var telephoneNumberChanged = originalTelephoneNumber !== telephoneNumberInput.value;
            var addressChanged = originalAddress !== addressInput.value;

            updateButton.disabled = !(isValidTelephoneNumber && isValidAddress && (isImageChanged || telephoneNumberChanged || addressChanged));
        }

        function validatePhoneNumber(telephoneNumber) {
            // Telefon numarasının uygunluğunu daha geniş bir regex ile kontrol etmek
            var phoneRegex = /^[0-9]{10,11}$/; // En az 10, en fazla 11 haneli
            return phoneRegex.test(telephoneNumber);
        }

        function validateAddress(address) {
            // Gerçek bir adres validasyonu, boşluk kontrolü yerine
            return address.trim() !== "";
        }

        function isFileSelected(input) {
            return input.files && input.files.length > 0;
        }
    });
</script>

@section Scripts{
    <partial name="_ValidationScriptsPartial" />
}

<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-validate/1.19.3/jquery.validate.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-validation-unobtrusive/3.2.12/jquery.validate.unobtrusive.min.js"></script>
